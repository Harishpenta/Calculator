# 🎨 Geometry Calculator - Visual Flow Diagrams

## 1. Component Architecture
```
┌─────────────────────────────────────────────────────────────────┐
│                         GEOMETRY FEATURE                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────┐         ┌─────────────────┐                │
│  │   HomeScreen   │────────>│  AppNavigation  │                │
│  └────────────────┘  route  └─────────────────┘                │
│         │                            │                           │
│         │                            ↓                           │
│         │                   ┌─────────────────┐                 │
│         │                   │  MainActivity   │                 │
│         │                   │  (creates VMs)  │                 │
│         │                   └─────────────────┘                 │
│         │                            │                           │
│         │                            ↓                           │
│         │              ┌──────────────────────────┐             │
│         │              │   GeometryViewModel      │             │
│         │              ├──────────────────────────┤             │
│         │              │ • State (data)           │             │
│         │              │ • Events (actions)       │             │
│         │              │ • calculate3D()          │             │
│         │              │ • calculate2D()          │             │
│         │              └──────────────────────────┘             │
│         │                            │                           │
│         │                            ↓                           │
│         │              ┌──────────────────────────┐             │
│         └─────────────>│   GeometryScreen (UI)    │             │
│                        ├──────────────────────────┤             │
│                        │ ┌──────────────────────┐ │             │
│                        │ │  Shape3DViewer       │ │             │
│                        │ │  (3D Graphics)       │ │             │
│                        │ └──────────────────────┘ │             │
│                        │ ┌──────────────────────┐ │             │
│                        │ │  Input Controls      │ │             │
│                        │ └──────────────────────┘ │             │
│                        │ ┌──────────────────────┐ │             │
│                        │ │  Results Card        │ │             │
│                        │ └──────────────────────┘ │             │
│                        └──────────────────────────┘             │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## 2. Data Flow (User Changes Dimension)
```
┌────────────────────────────────────────────────────────────────┐
│                      USER INTERACTION                           │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
                    User changes radius: 3 → 5
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│                     GeometryScreen.kt                           │
├────────────────────────────────────────────────────────────────┤
│  DimensionInput(                                                │
│    value = state.radius3D,                                      │
│    onValueChange = { newValue ->                                │
│      onAction(GeometryEvent.UpdateRadius3D(newValue))  ←────┐  │
│    }                                                        │  │
│  )                                                          │  │
└─────────────────────────────────────────────────────────────│──┘
                                                              │
                                                              │
                                                              ↓
┌────────────────────────────────────────────────────────────────┐
│                  GeometryViewModel.kt                           │
├────────────────────────────────────────────────────────────────┤
│  fun onEvent(event: GeometryEvent) {                            │
│    when (event) {                                               │
│      is UpdateRadius3D -> {                                     │
│        state = state.copy(radius3D = event.value) ←─────────┐  │
│        calculate3D() ←──────────────────────────────────────│──┤
│      }                                                       │  │
│    }                                                         │  │
│  }                                                           │  │
│                                                              │  │
│  private fun calculate3D() {                                 │  │
│    val volume = when (state.selected3DShape) {              │  │
│      SPHERE -> (4.0/3.0) * PI * radius³  ←─────────────────┐│  │
│      CUBE -> side³                                          ││  │
│      CYLINDER -> PI * radius² * height                      ││  │
│    }                                                         ││  │
│    state = state.copy(volume = volume) ←────────────────────┘│  │
│  }                                                            │  │
└───────────────────────────────────────────────────────────────┘  │
                              │                                    │
                              │ State Updated                      │
                              ↓                                    │
┌────────────────────────────────────────────────────────────────┐
│                  COMPOSE RECOMPOSITION                          │
│                  (Automatic UI Update)                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│                     GeometryScreen.kt                           │
│                     (Reads New State)                           │
├────────────────────────────────────────────────────────────────┤
│  Text("Volume: ${state.volume}")  ← Shows new value            │
│  Shape3DViewer(radius = state.radius3D)  ← Redraws bigger      │
└────────────────────────────────────────────────────────────────┘
```

## 3. 3D Rendering Pipeline
```
┌────────────────────────────────────────────────────────────────┐
│ STEP 1: DEFINE 3D COORDINATES                                   │
├────────────────────────────────────────────────────────────────┤
│  Cube corner at Point3D(x=2, y=2, z=2)                         │
│                                                                  │
│      z (depth)                                                  │
│      ↑                                                          │
│      │                                                          │
│      │    ●  ← point (2, 2, 2)                                 │
│      │   /│                                                     │
│      │  / │                                                     │
│      │ /  │                                                     │
│      └────────→ x                                               │
│     /                                                           │
│    ↓ y                                                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 2: ROTATE IN 3D SPACE                                      │
├────────────────────────────────────────────────────────────────┤
│  point.rotateX(25°)  // Tilt up                                │
│       .rotateY(45°)  // Spin right                             │
│       .rotateZ(0°)   // No roll                                │
│                                                                  │
│  Result: Point3D(x=1.2, y=2.8, z=1.5)                          │
│                                                                  │
│  Matrix math:                                                   │
│  ┌─────────────────────────┐                                   │
│  │ x' = x·cos - z·sin      │                                   │
│  │ y' = y                  │                                   │
│  │ z' = x·sin + z·cos      │                                   │
│  └─────────────────────────┘                                   │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 3: PERSPECTIVE PROJECTION (3D → 2D)                        │
├────────────────────────────────────────────────────────────────┤
│  fov = 500 (field of view)                                     │
│  scale = fov / (fov + z)  // Objects far away = smaller        │
│                                                                  │
│  screenX = centerX + x * scale                                 │
│  screenY = centerY - y * scale  // Flip Y (screen coords)      │
│                                                                  │
│  Example:                                                       │
│    z = 1.5 → scale = 500/(500+1.5) = 0.997                     │
│    x = 1.2 → screenX = 400 + 1.2*0.997 = 401.2                 │
│                                                                  │
│  Result: Offset(x=401, y=398) on screen                        │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│ STEP 4: DRAW ON CANVAS                                          │
├────────────────────────────────────────────────────────────────┤
│  Canvas {                                                       │
│    drawCircle(                                                  │
│      color = Color.Blue,                                       │
│      center = Offset(401, 398),                                │
│      radius = 5                                                │
│    )                                                            │
│  }                                                              │
│                                                                  │
│  Screen:                                                        │
│  ┌──────────────────────┐                                      │
│  │                      │                                      │
│  │        ●  ← dot here │                                      │
│  │                      │                                      │
│  └──────────────────────┘                                      │
└────────────────────────────────────────────────────────────────┘
```

## 4. Shape Selection Flow
```
                   ┌─────────────────────┐
                   │  GeometryScreen     │
                   │  (Shows all shapes) │
                   └─────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌─────────┐         ┌─────────┐        ┌─────────┐
   │  CUBE   │         │ SPHERE  │        │  CONE   │
   │  chip   │         │  chip   │        │  chip   │
   └─────────┘         └─────────┘        └─────────┘
        │                   │                   │
        │                   │ User taps         │
        │                   ↓                   │
        │         onAction(Select3DShape(SPHERE))
        │                   │                   │
        │                   ↓                   │
        │         ┌──────────────────┐          │
        │         │  ViewModel       │          │
        │         │  updates state   │          │
        │         └──────────────────┘          │
        │                   │                   │
        │                   ↓                   │
        │         state.selected3DShape = SPHERE
        │                   │                   │
        │                   ↓                   │
        │           calculate3D() runs          │
        │                   │                   │
        │                   ↓                   │
        │         Uses SPHERE formula:          │
        │         V = 4/3 π r³                  │
        │                   │                   │
        │                   ↓                   │
        │         ┌──────────────────┐          │
        │         │  UI Recomposes   │          │
        │         └──────────────────┘          │
        │                   │                   │
        │                   ↓                   │
        │      ┌───────────────────────┐        │
        │      │ Shape3DViewer draws   │        │
        │      │ sphere instead of cube│        │
        │      └───────────────────────┘        │
        │                   │                   │
        │                   ↓                   │
        │      ┌───────────────────────┐        │
        │      │ Input fields change:  │        │
        │      │ "Radius" (not "Side") │        │
        │      └───────────────────────┘        │
        │                   │                   │
        │                   ↓                   │
        │      ┌───────────────────────┐        │
        │      │ Results update with   │        │
        │      │ new volume/SA         │        │
        │      └───────────────────────┘        │
```

## 5. Auto-Rotation Animation Loop
```
┌────────────────────────────────────────────────────────────────┐
│                  state.isAutoRotating = true                    │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  LaunchedEffect(Unit) {                                         │
│    while (true) {                                               │
│      delay(16)  // ~60 FPS                                      │
│      onAction(UpdateRotationY(state.rotationY + 1f))           │
│    }                                                            │
│  }                                                              │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓  Every 16ms
┌────────────────────────────────────────────────────────────────┐
│  state.rotationY: 0° → 1° → 2° → ... → 359° → 0° (loop)       │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  Shape3DViewer gets new rotationY                               │
│    → Applies rotation to all vertices                          │
│    → Recalculates projections                                  │
│    → Redraws frame                                             │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
                     Smooth spinning animation!
```

## 6. Touch Drag Interaction
```
User touches screen and drags
        │
        ↓
┌────────────────────────────────────────┐
│  pointerInput(Unit) {                  │
│    detectDragGestures {                │
│      change, dragAmount ->             │
│                                        │
│      val deltaX = dragAmount.x        │
│      val deltaY = dragAmount.y        │
│                                        │
│      onRotationChange(                │
│        rotationY + deltaX * 0.5f,     │
│        rotationX - deltaY * 0.5f      │
│      )                                │
│    }                                  │
│  }                                    │
└────────────────────────────────────────┘
        │
        ↓
┌────────────────────────────────────────┐
│  Example:                              │
│  User drags RIGHT 100 pixels           │
│    → deltaX = 100                      │
│    → rotationY += 100 * 0.5 = 50°      │
│    → Cube rotates right                │
│                                        │
│  User drags UP 50 pixels               │
│    → deltaY = -50                      │
│    → rotationX -= (-50 * 0.5) = +25°   │
│    → Cube tilts up                     │
└────────────────────────────────────────┘
```

## 7. Lighting Calculation
```
┌────────────────────────────────────────────────────────────────┐
│                    LIGHTING SYSTEM                              │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  Light source at: Point3D(x=0, y=-1, z=-1)                     │
│  (Above and in front of viewer)                                │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  For each face:                                                 │
│                                                                  │
│  1. Calculate face normal (perpendicular vector)               │
│     ┌─────────┐                                                │
│     │    ↑    │  ← normal (pointing out)                       │
│     │    │    │                                                 │
│     │    face │                                                 │
│     └─────────┘                                                 │
│                                                                  │
│  2. Calculate angle between normal and light                   │
│     dot_product = normal · light_direction                     │
│                                                                  │
│  3. Determine brightness                                       │
│     brightness = max(0.3, dot_product)                         │
│     (0.3 = minimum ambient light)                              │
│                                                                  │
│  4. Apply to face color                                        │
│     final_color = base_color * brightness                      │
└────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌────────────────────────────────────────────────────────────────┐
│  RESULT:                                                        │
│                                                                  │
│  Front face facing light    → brightness = 1.0 (bright)        │
│  Side face at 45°           → brightness = 0.7 (medium)        │
│  Back face away from light  → brightness = 0.3 (dark/ambient)  │
└────────────────────────────────────────────────────────────────┘
```

## 8. Complete User Journey
```
START: User opens Calculator app
  │
  ↓
[Home Screen loads]
  │
  ├─ Shows categories: Algebra, Geometry, Finance...
  ├─ Shows calculator list
  │
  ↓
User taps "Geometry" category card
  │
  ↓
[Navigation System]
  │
  ├─ navController.navigate("geometry")
  ├─ AppNavigation routes to GeometryScreen
  │
  ↓
[GeometryScreen loads]
  │
  ├─ Reads state from GeometryViewModel
  ├─ Initial state: Cube with side=4
  │
  ↓
[Screen displays]
  │
  ├─ 3D Viewer shows rotating cube
  ├─ Shape selector chips (Cube selected)
  ├─ Input: "Side Length: 4 units"
  ├─ Results: "Volume: 64 cubic units"
  │
  ↓
User drags to rotate
  │
  ├─ Touch input detected
  ├─ rotationY updated
  ├─ Cube spins smoothly
  │
  ↓
User taps "Sphere" chip
  │
  ├─ Event sent: Select3DShape(SPHERE)
  ├─ ViewModel updates state
  ├─ calculate3D() runs with sphere formula
  │
  ↓
[UI Recomposes]
  │
  ├─ 3D Viewer draws sphere
  ├─ Input changes to "Radius: 3 units"
  ├─ Results: "Volume: 113.1 cubic units"
  │
  ↓
User changes radius: 3 → 5
  │
  ├─ Event: UpdateRadius3D(5.0)
  ├─ Recalculation: V = 4/3π(5³) = 523.6
  │
  ↓
[Instant update]
  │
  ├─ Sphere grows larger
  ├─ Results: "Volume: 523.6 cubic units"
  │
  ↓
User taps Share button
  │
  ├─ Creates intent with calculation text
  ├─ Opens share sheet
  │
  ↓
END: User shares or returns to home
```

## 9. State Management Pattern
```
┌────────────────────────────────────────────────────────────────┐
│                    UNIDIRECTIONAL DATA FLOW                     │
└────────────────────────────────────────────────────────────────┘

     STATE (Single Source of Truth)
          │
          │ read
          ↓
        [VIEW]  ─────────────┐
     GeometryScreen          │
     displays state          │
                             │ user interaction
                             │
                             ↓
                        [EVENT]
                   GeometryEvent.UpdateRadius(5.0)
                             │
                             │ sent to
                             ↓
                       [VIEWMODEL]
                     onEvent() processes
                             │
                             │ modifies
                             ↓
                         [STATE]
                    state = state.copy(...)
                             │
                             │ triggers
                             ↓
                      [RECOMPOSITION]
                    Compose framework
                         detects change
                             │
                             │ updates
                             ↓
                          [VIEW]
                    UI shows new values
                             │
                             └──────> (cycle repeats)
```

## 10. Memory Layout
```
┌────────────────────────────────────────────────────────────────┐
│                         MEMORY STRUCTURE                        │
└────────────────────────────────────────────────────────────────┘

MainActivity
  │
  ├─ GeometryViewModel (Heap)
  │   │
  │   ├─ state: GeometryState
  │   │   │
  │   │   ├─ selected3DShape: Shape3D.CUBE
  │   │   ├─ radius3D: 3.0 (Double, 8 bytes)
  │   │   ├─ height3D: 5.0 (Double, 8 bytes)
  │   │   ├─ volume: 64.0 (Double, 8 bytes)
  │   │   ├─ rotationX: 25f (Float, 4 bytes)
  │   │   ├─ rotationY: 45f (Float, 4 bytes)
  │   │   └─ isAutoRotating: true (Boolean, 1 byte)
  │   │
  │   └─ Functions
  │       ├─ onEvent()
  │       ├─ calculate3D()
  │       └─ calculate2D()
  │
  └─ Composable Tree (Stack/Heap)
      │
      ├─ GeometryScreen
      │   │
      │   ├─ Shape3DViewer
      │   │   │
      │   │   ├─ vertices: List<Point3D> (~100 points)
      │   │   ├─ faces: List<Face3D> (~50 faces)
      │   │   └─ Canvas drawing commands
      │   │
      │   ├─ Input controls (TextField, Slider)
      │   └─ Results cards (Text composables)
```

---

## 🎯 Key Takeaways

1. **State drives everything** - UI is a function of state
2. **Events are commands** - User actions become type-safe events
3. **Calculations are pure** - Given inputs, always same output
4. **3D is math** - Rotation matrices + perspective projection
5. **Recomposition is automatic** - Compose handles UI updates

---

**Want to dive deeper into any specific diagram? Let me know! 🚀**
